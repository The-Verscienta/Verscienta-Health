{
  "collectionName": "components_formula_ingredients",
  "info": {
    "displayName": "Formula Ingredient",
    "icon": "prescription-bottle"
  },
  "options": {},
  "attributes": {
    "herb": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::herb.herb"
    },
    "quantity": {
      "type": "decimal",
      "required": true
    },
    "unit": {
      "type": "enumeration",
      "enum": ["g", "mg", "oz", "ml", "tsp", "tbsp", "drops", "parts"],
      "required": true
    },
    "percentage": {
      "type": "decimal"
    },
    "role": {
      "type": "enumeration",
      "enum": ["chief", "deputy", "assistant", "envoy"]
    }
  }
}
